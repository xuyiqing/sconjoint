% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-signshare.R
\name{scmix_signshare}
\alias{scmix_signshare}
\title{Debiased sign shares for arbitrary coefficient contrasts}
\usage{
scmix_signshare(
  fit,
  contrasts,
  by = NULL,
  n_bins = 40L,
  M = 2000L,
  seed = 1L,
  sd_floor = 0.05,
  t_min = 2
)
}
\arguments{
\item{fit}{An \code{scmix} object.}

\item{contrasts}{One contrast or many: a named numeric vector
(expanded against \code{fit$attr_names}), a full length-p vector, a
numeric matrix with one contrast per row (rownames become labels),
or a list of such vectors (names become labels).}

\item{by}{Optional respondent grouping as in \code{\link[=scmix_average]{scmix_average()}}: a
respondent-length vector, a task-row-length vector, or a moderator
column name (median split).  Group estimates re-average the same
full-sample orthogonal signal; gated contrasts stay NA in every
group.}

\item{n_bins, M, seed}{Controls for the model-implied information
simulation (see \code{.scmix_information()}).}

\item{sd_floor}{Floor on the standardized residual SD along the
contrast (see \code{\link[=scmix_polarization]{scmix_polarization()}}).}

\item{t_min}{Threshold for the heterogeneity t-ratio gate; \code{0}
disables the rank and projection gates.}
}
\value{
An \code{scmix_quantity} with one entry per contrast (times
groups when \code{by} is given).  \verb{$extra} carries the parsed contrast
matrix, the per-fold \code{sigma_delta} values and their range, the
per-contrast gate table, and the labels gated to NA.
}
\description{
Estimates \verb{pi(delta) = E_Z[Pr(delta' beta_i > 0 | Z_i)]} for one or
many contrasts \code{delta}, with influence-function confidence intervals
from the same fit.  Under the maintained Gaussian factor law the
conditional share is \verb{Phi(delta' mu(Z) / sigma_delta)} with
\verb{sigma_delta^2 = delta' A A' delta} (ties at zero have probability
zero, so \code{>} and \code{>=} coincide).  The orthogonal signal carries the
location correction and the completed loading-influence term, with
both the indirect and the direct \code{dH/dA} channel.
}
\details{
Three recipes cover the paper's contrast estimands:
a compensating differential \code{Pr(beta_j + beta_k >= 0)} is
\code{contrasts = c(attr_j = 1, attr_k = 1)}; the majority preference
function \verb{Pr((X_A - X_B)' beta > 0)} takes a matrix whose rows are
the profile differences \code{X_A - X_B}; a slope share such as the tax
application's "share with a progressive schedule" puts the slope
weights on the rate coordinates -- centered log bracket midpoints
\code{log(c(5, 22.5, 60, 130, 275, 500))}, divided by their sum of
squares -- and reads off \code{pi(delta)} for the resulting linear
combination.

Reporting is gated per contrast (NA estimate, SE, and CI) by three
checks decided on scale-invariant quantities: a residual-SD floor on
the standardized index scale (any fold; reduces exactly to the
\code{\link[=scmix_polarization]{scmix_polarization()}} rule for a coordinate contrast); a
heterogeneity t-ratio \verb{delta' Sigma_bar delta / se >= t_min} from
the loading influence (a contrast whose dispersion rests on loading
coordinates this design does not identify cannot support a
population sign share); and a projection check that fails the
contrast when a material share of its gradient lies in loading
directions the correction projected out.  \code{t_min = 0} disables the
latter two.  \verb{$extra$gates} tabulates every gate input per contrast.

One \code{.scmix_prep()} (the information simulation, the expensive step)
serves the whole call, so batch the contrasts of a sweep into one
call rather than looping.
}
