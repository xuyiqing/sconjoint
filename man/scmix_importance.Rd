% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-importance.R
\name{scmix_importance}
\alias{scmix_importance}
\title{Model-implied attribute-importance shares from an scmix fit}
\usage{
scmix_importance(
  fit,
  design = c("uniform", "levels", "design_variance", "empirical"),
  levels = NULL,
  by = NULL,
  n_bins = 40L,
  M = 2000L,
  seed = 1L
)
}
\arguments{
\item{fit}{An \code{scmix} object.}

\item{design}{Level-weighting for the variance decomposition:
\code{"uniform"} (default; uniform draw over levels with the reference
at zero), \code{"levels"} (explicit numeric design levels for
single-column continuous attributes, others uniform),
\code{"design_variance"} (full block covariance of the realized
contrasts), or \code{"empirical"} (empirical level frequencies).}

\item{levels}{Named list of numeric design levels, used only with
\code{design = "levels"}.}

\item{by}{Optional respondent grouping as in \code{\link[=scmix_average]{scmix_average()}};
group shares renormalize within group (their own simplex
Jacobian), so a group's shares always sum to one.}

\item{n_bins, M, seed}{Controls for the model-implied information
simulation (see \code{.scmix_information()}).}
}
\value{
An \code{scmix_quantity} with one entry per attribute (times
groups when \code{by} is given).  \verb{$extra} carries the numerators, the
between-Z / residual split, the per-fold residual-term range, the
design tag, and the S blocks.
}
\description{
Decomposes utility variance by attribute from the fitted \verb{(mu, A)}
rather than from respondent-level point estimates: the numerator for
attribute g is \verb{E_Z[mu_g(Z)' S_g mu_g(Z)] + tr(S_g (AA')_\{gg\})} --
the model-implied average of the within-respondent attribute-utility
variance, whose residual-factor term a point-estimate decomposition
cannot see -- and the share is the ratio-of-means normalization
\verb{N_g / sum_h N_h} with simplex-Jacobian influence SEs.  \verb{$extra}
reports the \code{between_Z} (moderator-explained) and \code{residual}
(factor) components per attribute so applications can state which
totals they quote.
}
\details{
Two named deviations from \code{\link[=sc_importance]{sc_importance()}}: shares are
ratio-of-means (one normalization of population numerators), not the
plug-in's per-respondent share-then-average; and the default
weighting is \code{"uniform"} (the paper's reported convention for factor
designs; use \code{"levels"} with bracket midpoints for continuous
attributes, as in the tax application).
}
