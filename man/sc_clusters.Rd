% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantities-clusters.R
\name{sc_clusters}
\alias{sc_clusters}
\title{Preference clustering via k-means on \code{beta_hat(Z)}}
\usage{
sc_clusters(
  object,
  k = 3L,
  method = c("kmeans", "hclust"),
  scale = TRUE,
  nstart = 25L,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{An \code{sc_fit}.}

\item{k}{Integer number of clusters.  Default 3.}

\item{method}{Clustering method, currently only \code{"kmeans"} is
supported.  \code{"hclust"} is accepted as a synonym for average-
linkage hierarchical clustering on the same matrix and is
provided for reproducibility of prototype analyses.}

\item{scale}{Logical, scale columns of \code{beta_hat} before
clustering (default \code{TRUE}).}

\item{nstart}{Integer number of random k-means restarts (default
25), passed through to \code{stats::kmeans}.}

\item{seed}{Optional integer.  When supplied, the R RNG state is
set to \code{seed} before running k-means and restored on exit, so
the partition is reproducible.}

\item{...}{Unused; reserved for future extensions.}
}
\value{
An \code{sc_quantity} whose \code{estimate} is a named list with
fields \code{cluster_assignment} (length-N integer), \code{centers}
(k-by-p numeric), \code{within_ss} (length-k numeric), \code{sizes}
(length-k integer), and \code{total_within_ss} (scalar).
}
\description{
Run k-means clustering on the per-row \code{beta_hat} matrix of an
\code{sc_fit} and return the partition along with descriptive
statistics.  The algorithm is deterministic given \code{seed}; when
\code{seed = NULL} the assignment may vary across runs due to k-means
random initialization.
}
\details{
Columns of \code{beta_hat} are optionally standardized (\code{scale = TRUE},
the default) so that dummies with wider β ranges do not dominate
the Euclidean distance.  This matches the prototype's clustering
recipe in \verb{07b_structural_quantities.R} §7.
}
