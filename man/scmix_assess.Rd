% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paperps-assessment-predictions.R,
%   R/specification-assessment.R
\name{scmix_heldout_predictions}
\alias{scmix_heldout_predictions}
\alias{scmix_prediction_assessment}
\alias{scmix_design_benchmark}
\alias{scmix_design_benchmark_neutral}
\alias{scmix_structural_design_discrepancy}
\alias{scmix_design_audit}
\alias{scmix_heldout_sequence_score}
\alias{scmix_heldout_calibration}
\alias{scmix_completion_diagnostics}
\alias{scmix_rank_gate}
\alias{scmix_numerical_gate}
\alias{scmix_local_information}
\alias{scmix_profile_sequence_likelihood}
\alias{scmix_discrepancy_multiplier}
\alias{scmix_structural_sensitivity}
\alias{scmix_run_structural_sensitivity}
\alias{scmix_reporting_gates}
\alias{scmix_assess}
\alias{print.scmix_design_audit}
\alias{as.data.frame.scmix_design_audit}
\alias{print.scmix_reporting_gates}
\alias{as.data.frame.scmix_reporting_gates}
\alias{print.scmix_assessment}
\alias{as.data.frame.scmix_assessment}
\title{Out-of-fold mixed-logit predictions for specification assessment}
\usage{
scmix_heldout_predictions(
  fit,
  task_order = NULL,
  include_counts = TRUE,
  include_adjacent = TRUE,
  include_repeated = TRUE,
  repeat_tol = 1e-08
)

scmix_prediction_assessment(
  predictions,
  design_cell = NULL,
  respondent_group = NULL
)

scmix_design_benchmark(
  deltaX,
  y,
  respondent_id,
  contrast,
  protocol_probability,
  tol = 1e-08
)

scmix_design_benchmark_neutral(positive, negative)

scmix_structural_design_discrepancy(
  structural_estimate,
  design,
  structural_influence = NULL,
  target_label = NULL,
  structural_contrast = NULL,
  require_verified = TRUE
)

scmix_design_audit(
  contrasts,
  q,
  protocol_support = NULL,
  protocol_strata = NULL,
  deltaX = NULL,
  respondent_id = NULL,
  task_order = NULL,
  covariance_injective = NA,
  injectivity_certificate = NULL,
  tol = 1e-08
)

scmix_heldout_sequence_score(
  loglik,
  respondent_id = NULL,
  out_of_fold = FALSE,
  training_only_tuning = FALSE,
  provenance = NULL,
  analysis_signature = NULL
)

scmix_heldout_calibration(
  y,
  predicted,
  respondent_id,
  probability_breaks = seq(0, 1, by = 0.1),
  design_cell = NULL,
  respondent_group = NULL,
  task_order = NULL,
  joint = NULL,
  out_of_fold = FALSE,
  training_only_tuning = FALSE,
  provenance = NULL,
  analysis_signature = NULL
)

scmix_completion_diagnostics(
  completed_tasks,
  predictors = NULL,
  early_response = NULL,
  completion_pattern = NULL,
  respondent_id = NULL
)

scmix_rank_gate(
  Sigma,
  q,
  eigenvalue_margin = NULL,
  rank_tol = 1e-08,
  structural_scale = NULL,
  absolute_floor = sqrt(.Machine$double.eps)
)

scmix_numerical_gate(checks, tolerances, reference = nrow(checks))

scmix_local_information(
  score,
  structural_norm,
  respondent_id = NULL,
  direction_labels = colnames(score),
  identified_directions = FALSE,
  provenance = NULL,
  norm_tol = 1e-10
)

scmix_profile_sequence_likelihood(
  grid,
  loglik,
  respondent_id = NULL,
  direction = "structural direction",
  nuisance_reoptimized = FALSE,
  rank_boundary = FALSE,
  sieve_tuning_fixed = FALSE,
  provenance = NULL
)

scmix_discrepancy_multiplier(
  discrepancies,
  R = 999L,
  level = 0.95,
  seed = NULL,
  multipliers = NULL
)

scmix_structural_sensitivity(
  results = list(),
  q_values = NULL,
  materiality_tolerances = NULL,
  prespecified = FALSE
)

scmix_run_structural_sensitivity(
  runners,
  provenance,
  context = list(),
  q_values = NULL,
  materiality_tolerances = NULL,
  prespecified = FALSE
)

scmix_reporting_gates(
  mrs_denominator = NULL,
  mrs_margin = NULL,
  total_heterogeneity = NULL,
  total_margin = NULL,
  residual_variance = NULL,
  residual_variance_margin = NULL,
  confidence_interval = NULL,
  on_support = NA,
  rank_gate = NULL,
  numerical_gate = NULL,
  weak_information = NULL,
  local_information = NULL,
  information_eigenvalue_margin = NULL,
  target = c("generic_regular", "mrs", "variance_share", "threshold_share",
    "structural_choice", "descriptive"),
  inference = NULL,
  target_label = NULL,
  optimization_gate = NULL,
  design_audit = NULL
)

scmix_assess(
  fit = NULL,
  design_audit = NULL,
  design_benchmarks = NULL,
  discrepancies = NULL,
  heldout_scores = NULL,
  calibration = NULL,
  completion = NULL,
  local_information = NULL,
  profiles = NULL,
  numerical = NULL,
  sensitivity = NULL,
  reporting = NULL,
  inference = NULL,
  optimization = NULL,
  q = NULL,
  eigenvalue_margin = NULL,
  information_eigenvalue_margin = NULL,
  calibration_margins = NULL,
  required_reporting = NULL,
  required_discrepancies = NULL,
  discrepancy_tolerances = NULL
)

\method{print}{scmix_design_audit}(x, ...)

\method{as.data.frame}{scmix_design_audit}(x, ...)

\method{print}{scmix_reporting_gates}(x, ...)

\method{as.data.frame}{scmix_reporting_gates}(x, ...)

\method{print}{scmix_assessment}(x, ...)

\method{as.data.frame}{scmix_assessment}(x, ...)
}
\arguments{
\item{fit}{A paper-aligned \code{scmix} fit or \code{scmix_assemble_nested()} result.}

\item{task_order}{Optional task order; otherwise task-row order within each
respondent is used.}

\item{include_counts, include_adjacent, include_repeated}{Which joint
calibration constructors to compute.}

\item{repeat_tol}{Positive tolerance for exact ordered-contrast repetition.}

\item{predictions}{Result from \code{\link[=scmix_heldout_predictions]{scmix_heldout_predictions()}}.}

\item{design_cell, respondent_group}{Optional task-row strata.}

\item{deltaX}{Task-row contrast matrix.}

\item{y}{Task-row binary choices for alternative 1.}

\item{respondent_id}{Respondent identifiers, one per relevant row (task row
or score row, depending on the function); required or optional matching
each function's own default.}

\item{contrast}{Ordered contrast \code{d}; \code{-d} is not silently pooled with it.}

\item{protocol_probability}{Known \verb{bar(pi)_i(d)}, as one value, one per
respondent, one constant-within-respondent value per task, or a function
of respondent ids.}

\item{tol}{Positive relative singular-value tolerance for rank calculations
and absolute tolerance for realized contrast matching.}

\item{positive, negative}{Benchmark objects for \code{d} and \code{-d}.}

\item{structural_estimate}{Scalar structural estimate or a paper-aligned
\code{scmix_dml} result.}

\item{design}{A \code{scmix_design_benchmark} object.}

\item{structural_influence}{Optional centered influence contribution per
respondent, named by respondent id or ordered as in \code{design}.}

\item{target_label}{Required when \code{structural_estimate} is a \code{scmix_dml}
result; identifies the structural target and its confidence interval.}

\item{structural_contrast}{Ordered contrast used by the structural target.
When supplied it must equal the design benchmark's contrast.}

\item{require_verified}{If true, a \code{scmix_dml} result contributes joint
inference only when its global status is \code{"available"} or
\code{"conditional_available"}. The latter remains explicitly conditional on
documented high-level assumptions. In every case, the selected target must
itself be inference-available with a finite, strictly positive standard
error; otherwise the discrepancy is descriptive.}

\item{contrasts}{Matrix with one required ordered contrast per row.}

\item{q}{Prespecified maximum/active rank for the reported specification.}

\item{protocol_support}{Data frame with \code{contrast_id}, \code{stratum}, \code{event},
and known \code{probability}, or a contrast-by-stratum numeric matrix. The event
must be \code{"repeated_ordered_contrast"}.}

\item{protocol_strata}{Complete prespecified list of design/completion strata
that the protocol-support input must cover.}

\item{covariance_injective}{Optional classed verifier artifact for
rank-constrained injectivity. It must inherit from
\code{scmix_covariance_injectivity_certificate}, contain \code{established = TRUE}
and \code{verified = TRUE}, record nonempty \code{method}, \code{domain}, and \code{provenance}
fields, state a finite positive numeric \code{tolerance}, and contain the exact
\code{q} and contrast matrix verified. An unclassed list or bare logical
assertion is recorded but cannot establish the condition.}

\item{injectivity_certificate}{Legacy text field; text alone is not a
certificate and cannot establish injectivity.}

\item{loglik}{Respondent-by-grid matrix of integrated complete-sequence log
likelihoods.}

\item{out_of_fold}{Whether every score was computed from a fit that excluded
that respondent.}

\item{training_only_tuning}{Whether preprocessing and tuning for every fold
used only its training respondents.}

\item{provenance}{Named character vector/list giving a nonempty provenance
record for every supplied runner.}

\item{analysis_signature}{Optional nonempty signature linking fit-aware
calibration to the fitted analysis. The official prediction pipeline
supplies it.}

\item{predicted}{Held-out marginal choice-probability predictions, matched
row for row with \code{y}.}

\item{probability_breaks}{Prespecified bin boundaries from zero to one.}

\item{joint}{Optional data frame with \code{respondent_id}, \code{type}, \code{stratum},
\code{observed}, and \code{predicted} columns for genuine joint checks.}

\item{completed_tasks}{Completed task count, one per respondent.}

\item{predictors}{Optional respondent-level data frame.}

\item{early_response}{Optional respondent-level early-task response summary.}

\item{completion_pattern}{Optional respondent-level pattern label.}

\item{Sigma}{Fitted covariance.}

\item{eigenvalue_margin}{Prespecified positive separation margin; \code{NULL}
leaves regular inference unapproved.}

\item{rank_tol}{Relative numerical rank tolerance on the declared
structurally scaled covariance.}

\item{structural_scale}{Positive coefficient multipliers defining the
structural numerical scale, for example respondent-weighted contrast RMS
values. Required for \code{q > 0}.}

\item{absolute_floor}{Machine-level absolute eigenvalue floor after scaling.}

\item{checks}{Data frame with one row per numerical replication/refinement.}

\item{tolerances}{Named nonnegative tolerances for assessed numeric columns.}

\item{reference}{Row used as the reference, by default the last row.}

\item{score}{Respondent-by-direction matrix of complete-sequence directional
scores.}

\item{structural_norm}{Positive-definite Gram matrix for the fixed
structural norm used to normalize directions.}

\item{direction_labels}{Optional labels for score columns.}

\item{identified_directions}{Must be explicitly \code{TRUE}.}

\item{norm_tol}{Numerical positive-definiteness tolerance.}

\item{grid}{Numeric profile values.}

\item{direction}{Description of the structural or quantity direction.}

\item{nuisance_reoptimized}{Must be explicitly true if the table is to be
labeled a profile rather than a slice.}

\item{rank_boundary}{Whether the profile touches a covariance-rank boundary.}

\item{sieve_tuning_fixed}{Whether the sieve architecture, tuning rule, and
penalty were fixed over the profile grid.}

\item{discrepancies}{One discrepancy object or a named list of them.}

\item{R}{Number of multiplier replications.}

\item{level}{Simultaneous confidence level.}

\item{seed}{Optional seed.}

\item{multipliers}{Optional precomputed \code{R}-by-\code{N} mean-zero multiplier
matrix; useful for deterministic tests.}

\item{results}{Named list keyed by the required component names shown in the
returned status table. Each supplied component must contain an enumerated
\code{status}, nonempty \code{provenance}, and (when run) a nonnull \code{result}.}

\item{q_values}{Prespecified primary and alternative rank values.}

\item{materiality_tolerances}{Prespecified nonnegative tolerance named for
every implemented component. For substantive readiness, each result must
record \code{tolerance_applied = TRUE}, the matching \code{materiality_tolerance}, a
finite nonnegative scalar \code{materiality_value}, and \code{passed = TRUE} exactly
when that value does not exceed the tolerance.}

\item{prespecified}{Whether the plan was fixed before viewing outcomes.}

\item{runners}{Named list of functions keyed by sensitivity component. Each
function receives \code{context} and returns a result. A result may include
\code{materially_sensitive = TRUE} or \code{passed = FALSE} to set its status.}

\item{context}{Read-only application inputs and fitted objects passed to each
callback.}

\item{mrs_denominator, total_heterogeneity, residual_variance}{Optional values.}

\item{mrs_margin, total_margin, residual_variance_margin}{Prespecified margins.}

\item{confidence_interval}{Optional interval used descriptively. A regular
majority claim is generated only from \code{inference} and \code{target_label}.}

\item{on_support}{Whether the target is on randomized support.}

\item{rank_gate, numerical_gate}{Optional gate objects.}

\item{weak_information}{Deprecated logical warning. It is recorded for
compatibility but cannot establish adequate information by itself.}

\item{local_information}{A verified \code{scmix_local_information} object.}

\item{information_eigenvalue_margin}{Prespecified strictly positive minimum
acceptable structural-norm generalized information eigenvalue.}

\item{target}{Target type determining the evidence that must be supplied.}

\item{inference}{Paper-aligned \code{scmix_dml} result.}

\item{optimization_gate}{Output of \code{scmix_optimization_audit()}.}

\item{design_audit}{Optional design audit used to label theorem applicability.}

\item{design_benchmarks, heldout_scores, calibration, completion, profiles, numerical, sensitivity, reporting}{Precomputed assessment objects. When \code{fit} is
supplied, only \code{fit} itself is used directly (to obtain covariance and
\code{q} for a rank gate); every other component here is a precomputed
object produced by its own constructor (for example \code{design_audit} by
\code{\link[=scmix_design_audit]{scmix_design_audit()}}, \code{discrepancies} by
\code{\link[=scmix_structural_design_discrepancy]{scmix_structural_design_discrepancy()}}, \code{local_information} by
\code{\link[=scmix_local_information]{scmix_local_information()}}) and is only assembled and status-checked
here, not recomputed.}

\item{optimization}{Optimization audit.}

\item{calibration_margins}{Named positive materiality thresholds \code{marginal}
and \code{joint} for the maximum absolute held-out calibration gaps.}

\item{required_reporting}{Prespecified nonempty names of all quantities for
which regular structural reporting is requested.}

\item{required_discrepancies}{Prespecified names of all on-support
structural--design comparisons. \code{character(0)} explicitly declares none;
\code{NULL} leaves scope undeclared and fails closed.}

\item{discrepancy_tolerances}{Named positive materiality thresholds for every
required structural--design discrepancy.}

\item{x}{An object to print or convert with \code{as.data.frame()}.}

\item{...}{Unused.}
}
\value{
A fit-aware prediction object consumable by
\code{\link[=scmix_heldout_sequence_score]{scmix_heldout_sequence_score()}} and \code{\link[=scmix_heldout_calibration]{scmix_heldout_calibration()}}, carrying
the fitted analysis's nonempty \code{analysis_signature}.

Held-out sequence-score and calibration objects carrying the same
nonempty \code{analysis_signature} as the fit-aware prediction object.

An internal object containing the estimate, respondent influence
contributions, standard error, match counts, and protocol probabilities.

An internal \code{scmix_design_benchmark} object.

An internal discrepancy object; without structural influence the
comparison is explicitly descriptive.

An internal design-audit object.

Means, respondent standard errors, and paired model differences.

An internal calibration object.

An internal completion-assessment object.

An internal gate object.

An internal numerical gate object.

Generalized eigenvalues/eigenvectors and the empirical structural
information matrix. This is a local information diagnostic, not a global
identification test.

A descriptive profile table.

Multiplier draws, simultaneous critical value, and intervals.

A fail-closed status table, supplied results, protocol-completion
status, and a separate substantive-pass indicator. Completion requires at
least two distinct prespecified rank values; substantive passage also
requires auditable component-level materiality evidence. Neither verifies
a maintained assumption.

A \code{scmix_structural_sensitivity} object containing every result or
captured error. Missing runners remain visibly \code{not_run}.

A gate table and allowed majority label.

An internal \code{scmix_assessment} object. \code{protocol_complete} records
execution only; \code{structural_reporting_ready} additionally applies all
prespecified materiality and reporting gates. Neither verifies maintained
assumptions.
}
\description{
Computes marginal task probabilities, complete-sequence log scores, the
response-count distribution, adjacent-task response-pattern probabilities,
and (when realized) exact repeated-contrast response-pattern probabilities.
Every prediction for fold k uses only the nuisance fit trained outside fold
k, with one shared latent factor draw across the respondent's entire
sequence.

Implements the respondent Horvitz--Thompson contribution in the paper.
Respondents with no realized matching task are retained with a zero
numerator. \code{protocol_probability} is the known conditional expectation of
the respondent's matching-task fraction, including all assignment and
completion strata used by the protocol.

Combines separately estimated benchmarks for \code{d} and \code{-d} as
\code{0.5 * {V(d) + 1 - V(-d)}} while retaining their respondent covariance.

Subtracts a design benchmark from a structural one-step estimate. When the
structural respondent influence contributions are supplied, uncertainty is
computed from the respondent-by-respondent difference, preserving their
covariance. It never treats the estimates as independent.

Checks affine rank, the transparent unrestricted-vech sufficient condition
for covariance injectivity, user-supplied protocol support, and realized
first-two-task repeat counts. A failed or incomplete audit makes the current
theorem silent; it is never reported as proof of nonidentification.

Accepts one already-integrated complete-sequence log likelihood per
respondent and model. Duplicate respondent ids are rejected so task-level
log scores cannot be silently substituted.

Every summary averages within respondent before averaging across
respondents. Optional \code{joint} rows allow response-pattern, transition, or
repeated-contrast probabilities to be assessed without pretending they can
be reconstructed from marginal predictions alone.

Describes associations between completed-task counts and observed
respondent predictors, completion patterns, and early responses. These are
diagnostics for observed selection and cannot verify independence from
latent preferences or utility shocks.

Checks whether a fitted covariance lies on the prespecified exact-rank
stratum and whether its smallest active eigenvalue exceeds a prespecified
margin. This is a warning rule, not a regular rank test.

Compares estimates, standard errors, held-out scores, and covariance
eigenvalues across integration refinements, independent scrambles,
rotations, or starts. Tolerances must be prespecified by column.

Computes generalized information eigenvalues from caller-supplied
complete-sequence directional scores and a structural-norm Gram matrix. The
caller must affirm that columns are identified structural function/covariance
directions with network reparameterizations and factor rotations already
quotiented out. Raw neural-weight or loading-factor Hessians are rejected by
construction because this interface accepts scores, not a Hessian.

Summarizes one complete-sequence log likelihood per respondent and grid
point after the caller has reoptimized the remaining nuisance components.
The output is descriptive; it assigns no likelihood-ratio critical values,
particularly at a covariance-rank boundary.

Applies the same multiplier draw to each respondent across every
prespecified contrast, preserving both structural--design covariance and
cross-contrast covariance. Inputs must be discrepancy objects with joint
respondent influence contributions.

Records whether every sensitivity required by the paper was implemented,
run, or omitted. Supplied alternative-model results remain sensitivity
analyses unless a separate identification result and description are
explicitly provided; they are never promoted to coequal identified models
by this collector.

Runs application-specific callbacks and converts their outputs into the
fail-closed schema used by \code{\link[=scmix_structural_sensitivity]{scmix_structural_sensitivity()}}. The primary
normal low-rank mixed logit remains the sole baseline model; callbacks are
assessment tools and do not inherit its identification theorem.

Applies the paper's MRS-denominator, total-heterogeneity,
threshold-variance, majority, support, weak-information, and numerical/rank
rules. Missing margins fail safely by leaving the corresponding claim
unapproved.

Assembles independently computed design, benchmark, predictive,
calibration, completion, numerical, and reporting objects. If a fitted
covariance is available, it also constructs the rank-interiority gate.
The collector deliberately does not convert diagnostic success into a claim
that maintained structural assumptions have been verified.
}
\details{
When \code{fit} is supplied, its nonempty \code{analysis_signature} must match
the inference, optimization, and numerical artifacts. Every supplied
held-out score and calibration object must carry the same nonempty
signature. The status
\code{"conditional_available"} authorizes only explicitly conditional claims;
it never says the high-level assumptions were verified.
}
