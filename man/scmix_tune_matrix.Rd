% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paperps-computation.R
\name{scmix_tune_matrix}
\alias{scmix_tune_matrix}
\alias{scmix_tune_outer_matrix}
\alias{scmix_assemble_nested}
\alias{scmix_optimization_audit}
\alias{scmix_integration_refinement}
\alias{scmix_q_sensitivity}
\title{Respondent-level inner cross-validation for one outer training sample}
\usage{
scmix_tune_matrix(
  deltaX,
  y,
  Z,
  respondent_id,
  grid,
  q = NULL,
  K = 3L,
  allow_q_tuning = FALSE,
  allow_integration_tuning = FALSE,
  n_epochs = 400L,
  learning_rate = 0.01,
  n_starts = 2L,
  mu_bound = 10,
  kappa_bound = 10,
  a_bound = 10,
  weight_bound = 10,
  opt_tol = 1e-07,
  grad_tol = 1e-04,
  seed = NULL,
  device = "cpu",
  refit = TRUE,
  refit_integration_grid = NULL,
  keep_cv_fits = FALSE,
  verbose = FALSE
)

scmix_tune_outer_matrix(
  deltaX,
  y,
  Z,
  respondent_id,
  grid,
  q = NULL,
  outer_K = 5L,
  inner_K = 3L,
  outer_fold_id = NULL,
  seed = NULL,
  ...
)

scmix_assemble_nested(
  nested,
  attr_names = NULL,
  z_names = NULL,
  require_optimization_gate = TRUE,
  diagnostic_only = FALSE
)

scmix_optimization_audit(fit)

scmix_integration_refinement(
  resolutions,
  scrambles = NULL,
  refitter,
  extractors,
  tolerances,
  reference = NULL,
  keep_fits = FALSE
)

scmix_q_sensitivity(
  primary_q,
  alternatives,
  refitter,
  extractors,
  keep_fits = FALSE
)
}
\arguments{
\item{deltaX, y, Z, respondent_id}{Task-level matrix inputs.}

\item{grid}{Data frame or list of candidate lists. Each candidate specifies
\code{hidden}, \code{weight_decay}, and optionally integration controls and \code{q}.
QMC draw counts must be even because tuning uses antithetic draws.}

\item{q}{Optional fixed prespecified factor rank shared by every candidate.}

\item{K}{Number of respondent-level inner folds.}

\item{allow_q_tuning}{Permit data-dependent rank tuning. The paper's regular
fixed-specification inference should leave this \code{FALSE}.}

\item{allow_integration_tuning}{Permit integration settings in learner CV to
vary only as an explicitly diagnostic exercise. Numerical resolution
should ordinarily be held fixed here and studied with fresh refits via
\code{\link[=scmix_integration_refinement]{scmix_integration_refinement()}}.}

\item{n_epochs, learning_rate, n_starts, mu_bound, kappa_bound, opt_tol, grad_tol, seed, device, verbose}{Optimization controls, passed to \code{\link[=scmix_tune_outer_matrix]{scmix_tune_outer_matrix()}} via its \code{...}.}

\item{a_bound}{Positive raw-coefficient Frobenius bound for the loading.}

\item{weight_bound}{Positive coordinatewise network-parameter bound.}

\item{refit}{Refit the selected specification on all supplied rows. These
rows must themselves be one outer training sample when used for DML.}

\item{refit_integration_grid}{Optional common integration grid for the
selected refit. The nested outer wrapper uses this to ensure every fold
nuisance is evaluated on exactly the same finite nodes.}

\item{keep_cv_fits}{Retain all inner-fold network objects (memory intensive).}

\item{outer_K, inner_K, outer_fold_id}{Respondent-level outer/inner fold
controls, mirroring \code{\link[=scmix_tune_matrix]{scmix_tune_matrix()}}'s \code{K} one level up: each outer
fold runs its own complete inner tuning.}

\item{...}{Core optimization controls (\code{n_epochs}, \code{learning_rate},
\code{n_starts}, \code{mu_bound}, \code{kappa_bound}, \code{opt_tol}, \code{grad_tol}, \code{seed},
\code{device}, \code{verbose}) forwarded to \code{\link[=scmix_tune_matrix]{scmix_tune_matrix()}} inside every
outer training set.}

\item{nested}{Result from \code{\link[=scmix_tune_outer_matrix]{scmix_tune_outer_matrix()}}.}

\item{attr_names, z_names}{Optional labels for contrast and moderator columns.}

\item{require_optimization_gate}{Fail unless every selected outer refit has
finite objective components, satisfies the fresh returned-state criterion
and gradient tolerances, and has complete diagnostics showing that no
artificial compact-parameter bound is active. It may be disabled only
together with \code{diagnostic_only = TRUE}.}

\item{diagnostic_only}{Permit assembly after a failed computational or
data-dependent-selection gate solely for diagnostics. Such an object is
explicitly marked ineligible for the paper's ordinary inference.}

\item{fit}{An \code{scmix}, \code{scmix_tuned_matrix_fit}, or assembled nested fit.}

\item{resolutions}{Positive integer node/draw resolutions.}

\item{scrambles}{Scramble seeds or labels. \code{NULL} requests one deterministic
setting per resolution.}

\item{refitter}{Function taking one integer q and returning a fit.}

\item{extractors}{Named QOI/SE/score/eigen extractor functions.}

\item{tolerances}{Named tolerances for the flattened extractor columns.}

\item{reference}{Reference row for \code{scmix_numerical_gate}.}

\item{keep_fits}{Retain the refits.}

\item{primary_q}{Rank prespecified for the paper's primary specification.}

\item{alternatives}{Small set of sensitivity ranks.}
}
\value{
An internal tuning object with respondent-sequence scores,
training-only preprocessing, inner-fold computational and candidate
eligibility diagnostics, selected specification, optional refit, and a
data/specification/fold analysis signature.

A fold-nuisance object directly consumable by \code{\link[=scmix_dml]{scmix_dml()}}. It is
not a full-sample structural plug-in fit and retains the nested analysis
signature.

Start-by-fit diagnostics, compact-bound gates, the fit analysis
signature, and scope summaries. Bound activity is available for the
selected start because the core fit does not retain all nonselected
networks.

Settings, recomputed checks, numerical gate, fit-linked analysis
signature status, and optional fits.

Sensitivity table. The primary q is never replaced by the best-looking
alternative and no selection-adjusted coverage is claimed.
}
\description{
This matrix interface compares a finite, prespecified tuning grid using the
unpenalized complete-sequence log likelihood. Every row for a respondent is
assigned to the same validation fold. Moderator transforms and contrast
scales are learned on inner-training respondents only and frozen before
validation. For DML, the entire call must occur inside an outer training
sample; running it once on the full sample and reusing the selected nuisance
architecture in every outer fold leaks held-out information.
A candidate is eligible for selection only when every inner-fold fit passes
the fail-closed optimization and compact-bound gate and every validation
score is finite. Scores and failure diagnostics for excluded candidates are
retained, and tuning stops rather than selecting if no candidate is eligible.

Converts one selected refit from each outer training set into the exact
fold-nuisance fields consumed by \code{\link[=scmix_dml]{scmix_dml()}}. Every fold network is
evaluated on all respondents using only that outer-training fold's frozen
preprocessing. The current inference engine evaluates one common finite
integration grid, so the assembler requires identical nodes and weights
across folds and also retains them individually for audit.

Calls \code{refitter(setting)} once for every resolution-by-scramble setting,
then calls every requested extractor on every new fit. The resulting QOI,
standard-error, held-out-score, and information-eigenvalue summaries are
passed to \code{scmix_numerical_gate}; no cached baseline estimate is recycled.
}
