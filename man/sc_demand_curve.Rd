% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantities-demand.R
\name{sc_demand_curve}
\alias{sc_demand_curve}
\title{Aggregate demand curve as a function of a numeric cost attribute}
\usage{
sc_demand_curve(
  object,
  cost_attr,
  cost_grid = NULL,
  profile = NULL,
  subgroup = NULL
)
}
\arguments{
\item{object}{An \code{sc_fit}.}

\item{cost_attr}{Character name of the numeric cost attribute.
Must correspond to a single column in \code{object$attr_names}.}

\item{cost_grid}{Numeric vector of cost values to evaluate.
Default: 20-point regular grid over the observed range of
\code{cost_attr} in \code{object$deltaX}.}

\item{profile}{Optional named list for non-cost attributes
(defaults to reference levels, i.e. all zeros).}

\item{subgroup}{Optional row selector.}
}
\value{
An \code{sc_quantity} whose \code{estimate} is a data.frame with
columns \code{cost}, \code{demand}, and \code{se}.
}
\description{
Sweeps a grid of values for a numeric attribute (typically cost or
price) while holding other profile attributes fixed, computing the
average choice probability at each grid point.  The reference
profile is the all-reference-level alternative.
}
\details{
For each cost value \eqn{c} and respondent \eqn{i}:
\deqn{V_i(c) = \hat\beta_{\mathrm{cost}}(Z_i) \cdot c +
  \sum_{k \neq \mathrm{cost}} \hat\beta_k(Z_i) \cdot
  \mathrm{profile}_k}
\deqn{\mathrm{demand}(c) = \frac{1}{N_S}\sum_{i\in S}
  \mathrm{plogis}(V_i(c))}
}
