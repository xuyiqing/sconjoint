% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantities-subgroup.R
\name{sc_subgroup}
\alias{sc_subgroup}
\title{Subgroup-conditional preference estimates}
\usage{
sc_subgroup(object, subgroup, ...)
}
\arguments{
\item{object}{An \code{sc_fit} produced by \code{scfit()}.}

\item{subgroup}{Subgroup selector (see Details).}

\item{...}{Unused; present for future extensions.}
}
\value{
An \code{sc_quantity} with \code{estimate} a data.frame containing
one row per dummy (\code{dummy_name}, \code{theta}, \code{se}, \code{ci_lo}, \code{ci_hi}),
or a named list of \code{sc_quantity} objects if \code{subgroup} was a
named list.
}
\description{
Re-average \code{beta_hat(Z_i)} over a user-specified subset of
respondents and return point estimates with respondent-clustered
standard errors.  The subgroup argument accepts several forms:
\itemize{
\item a logical vector of length \code{N = nrow(object$beta_hat)}
(one flag per row of the DML output);
\item an integer vector of row indices;
\item a length-1 character naming a Z column (rows with
\code{Z[, col] > median(col)} are selected -- same rule as the
Tier A \code{subgroup = "z_col"} helper);
\item a function of a single argument \code{Z} (the N-by-p_Z matrix
on the \code{sc_fit}) that returns a logical vector of length \code{N};
\item a named list of any of the above, in which case the
function is applied to each element and the return value is a
named list of \code{sc_quantity} objects.
}
}
\examples{
\donttest{
if (requireNamespace("torch", quietly = TRUE) &&
    torch::torch_is_installed()) {
  ## See vignette("sconjoint") for a full worked example.
}
}
}
