% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-quantities.R
\name{scmix_mrs}
\alias{scmix_mrs}
\title{Debiased population MRS from an scmix fit}
\usage{
scmix_mrs(fit, numerator, denominator, n_bins = 40L, M = 2000L, seed = 1L)
}
\arguments{
\item{fit}{An \code{scmix} object.}

\item{numerator, denominator}{Character of form \code{"attribute:level"},
a bare dummy column name, or an integer column index (as in
\code{\link[=sc_mrs]{sc_mrs()}}).}

\item{n_bins, M, seed}{Controls for the model-implied information
simulation (see \code{.scmix_information()}).}
}
\value{
An \code{scmix_quantity} with the scalar ratio; \verb{$extra} carries
the Fieller interval and the component estimates.
}
\description{
Estimates \code{MRS_jk = theta_j / theta_k} on the integrated-likelihood
route: both coordinates come from the corrected \code{\link[=scmix_theta]{scmix_theta()}}
signal, the SE is the delta-method standard error using the joint
respondent-level influence (the cross-covariance between coordinates
is free, since the signal is stored as an N x p matrix), and a
Fieller interval is returned in \verb{$extra}. A warning fires when the
denominator is weakly separated from zero (|theta_k|/SE < 4), the
regime in which the delta-method interval undercovers; the memo's
reporting rule is to use the largest-|theta| attribute as the
denominator.
}
