% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-quantities.R
\name{scmix_design_check}
\alias{scmix_design_check}
\title{Design-rank preflight for distributional claims}
\usage{
scmix_design_check(
  fit,
  n_bins = 40L,
  M = 2000L,
  seed = 1L,
  eig_tol = 0.05,
  t_min = 2
)
}
\arguments{
\item{fit}{An \code{scmix} object.}

\item{n_bins, M, seed}{Controls for the model-implied information
simulation (see \code{.scmix_information()}).}

\item{eig_tol}{Relative eigenvalue below which a loading direction
is reported as weakly identified (default 0.05).}

\item{t_min}{Loading t-ratio below which a coordinate is reported as
not supporting distributional claims (default 2).}
}
\value{
An object of class \code{scmix_design_check}: a list with
\code{spectrum} (standardized eigenvalues, relative to the largest),
\code{loadings} (data frame: coordinate, standardized loading, SE, t),
\code{weak_directions}, \code{identified}, and the thresholds used.
}
\description{
Computes the model-implied EFFECTIVE loading information at the
design in hand --- the average of
\verb{I_AA(Z) - I_Amu(Z) I_mumu(Z)^\{-1\} I_muA(Z)} over respondents, the
matrix whose inverse scales the loading-influence correction --- and
reports (i) its eigen-spectrum on the standardized (index) scale and
(ii) per-loading standard errors and t-ratios. Distributional
estimands (sign shares \code{pi_k}, residual SDs) are identified only in
the loading directions this design actually pins down; coordinates
whose loading t-ratio is small cannot support population
distributional claims at this design, whatever the point estimates
say. Run this before reporting \code{\link[=scmix_polarization]{scmix_polarization()}} or residual-SD
magnitudes.
}
\details{
The finite-design rank analysis behind this check (task T5 of the
estimand memo): on the bundled applications it identifies the tax
design (T = 8) up to one weak direction, while the candidate design
(T = 3) pins down only the agenda coordinates' loadings.
}
