% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paperps-quantities.R
\name{scmix_paper_theta}
\alias{scmix_paper_theta}
\alias{scmix_paper_choice}
\alias{scmix_paper_mrs}
\alias{scmix_paper_signshare}
\alias{scmix_paper_compensating}
\alias{scmix_paper_heterogeneity}
\alias{scmix_paper_quantities}
\title{Respondent-weighted average and subgroup preferences}
\usage{
scmix_paper_theta(fit, subgroup = NULL, mu = NULL)

scmix_paper_choice(
  fit,
  contrast,
  position_neutral = FALSE,
  kappa = NULL,
  mu = NULL,
  Sigma = NULL,
  n_nodes = 31L,
  on_support = NA
)

scmix_paper_mrs(
  fit,
  numerator,
  denominator,
  denominator_margin = NULL,
  mu = NULL
)

scmix_paper_signshare(
  fit,
  contrast,
  ties = c("exclude", "include"),
  variance_margin = NULL,
  ci = NULL,
  mu = NULL,
  Sigma = NULL
)

scmix_paper_compensating(
  fit,
  penalty,
  benefit,
  amount,
  variance_margin = NULL,
  ci = NULL,
  mu = NULL,
  Sigma = NULL
)

scmix_paper_heterogeneity(
  fit,
  direction = NULL,
  total_margin = NULL,
  mu = NULL,
  Sigma = NULL
)

scmix_paper_quantities(
  fit,
  subgroup = NULL,
  choice = NULL,
  mrs = NULL,
  sign = NULL,
  compensating = NULL,
  heterogeneity = NULL
)
}
\arguments{
\item{fit}{A mixed-logit fit.}

\item{subgroup}{Optional subgroup vector for average preferences.}

\item{contrast}{Direction \code{c}.}

\item{position_neutral}{If true, averages the two display positions.}

\item{kappa, mu, Sigma}{Optional explicit structural estimates.}

\item{n_nodes}{One-dimensional normal quadrature resolution.}

\item{on_support}{\code{TRUE}, \code{FALSE}, or \code{NA} after the design audit.}

\item{numerator, denominator}{Contrasts \code{c_a} and \code{c_b}.}

\item{denominator_margin}{Prespecified lower bound on the absolute
denominator. \code{NULL} leaves the reporting gate unconfigured.}

\item{ties}{Whether exact zero is excluded or included.}

\item{variance_margin}{Prespecified lower bound on directional residual
variance for regular inference. \code{NULL} leaves the gate unconfigured.}

\item{ci}{Optional two-element confidence interval for the majority gate.}

\item{penalty, benefit}{Contrasts \code{c_p} and \code{c_b}.}

\item{amount}{Benefit amount \code{a}.}

\item{direction}{Optional contrast \code{c}.}

\item{total_margin}{Prespecified lower bound on total directional
heterogeneity before reporting its explained share.}

\item{choice, mrs, sign, compensating, heterogeneity}{Optional named lists of
arguments for the corresponding quantity routine.}
}
\value{
An internal \code{scmix_paper_quantity} object.

An internal \code{scmix_paper_quantity} object.

An internal \code{scmix_paper_quantity} object.

An internal \code{scmix_paper_quantity} object.

An internal \code{scmix_paper_quantity} object.

An internal \code{scmix_paper_quantity} object.

A named list of \code{scmix_paper_quantity} objects.
}
\description{
Computes the paper's plug-in quantities from fitted conditional means.
Respondent posterior summaries are never used. When \code{subgroup} is supplied,
it must be prespecified and have one value per respondent or task.

Integrates the logit choice probability over the fitted normal preference
distribution and then averages equally over respondents.

Computes \code{-tau(c_a) / tau(c_b)} and applies a prespecified substantive
denominator margin. The estimate is retained for audit even when the
reporting gate fails.

At positive directional residual variance the conditional share is a normal
CDF. At zero variance, \code{ties = "exclude"} implements the strict sign-share
inequality and \code{ties = "include"} implements the compensating-share
convention.

Computes \verb{Pr\{(c_p + a c_b)' beta >= 0\}} and therefore counts exact ties as
compensated when directional residual variance is zero.

Computes the respondent-weighted between-covariate component, residual
component, total component, and optional directional variance share.

Each optional specification is a named argument list passed to its
corresponding \verb{scmix_paper_*} routine. This is a convenience collector, not
an inference routine; regular intervals require the cross-fitted one-step
procedure described in the paper.
}
