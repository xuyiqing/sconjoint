% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-diagnostics.R
\name{scmix_linearity_check}
\alias{scmix_linearity_check}
\title{Second profile-Newton linearity diagnostic for the loading estimate}
\usage{
scmix_linearity_check(fit, n_bins = 40L, M = 2000L, seed = 1L)
}
\arguments{
\item{fit}{An \code{scmix} object.}

\item{n_bins, M, seed}{Controls for the model-implied information
simulation (see \code{.scmix_information()}).}
}
\value{
An object of class \code{scmix_linearity_check}: \code{delta1} and
\code{delta2} (p x q, raw units), their standardized norms \code{norm1_std}
and \code{norm2_std}, \code{ratio = norm2_std / norm1_std}, \code{max_rel_se}
(largest standardized second-increment coordinate over its loading
SE), and \code{n_truncated} (loading directions the correction projects
out; increments live in the kept subspace).
}
\description{
The shipped loading-influence correction is algebraically a one-step
profile-likelihood Newton step on vec(A); treating \code{A_hat} as
asymptotically linear rests on that one step being enough.  This
diagnostic takes the free first increment
\verb{delta_1 = mean_i IF_A[i, ]}, applies it to every fold loading, and
computes the SECOND increment \code{delta_2} from the effective scores at
the incremented loadings, holding the information blocks at their
base values (their dependence on the perturbation is second order,
which keeps the diagnostic at roughly one preparation cost).  A
small second increment -- \code{ratio} well below one and \code{max_rel_se}
well below one -- supports the linearity assumption behind the
correction; reporting only, no hard threshold.
}
