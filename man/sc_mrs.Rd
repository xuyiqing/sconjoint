% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantities-mrs.R
\name{sc_mrs}
\alias{sc_mrs}
\title{Marginal rate of substitution between two attribute dummies}
\usage{
sc_mrs(
  object,
  numerator,
  denominator,
  estimand = c("individual", "population"),
  trim = c(0.01, 0.99),
  subgroup = NULL,
  which_beta = c("hybrid", "dnn")
)
}
\arguments{
\item{object}{An \code{sc_fit}.}

\item{numerator, denominator}{Character of form \code{"attribute:level"},
or a bare dummy column name matching \code{object$attr_names}, or an
integer column index.}

\item{estimand}{Either \code{"individual"} (default) for the
respondent-averaged trimmed ratio of per-respondent
\eqn{\beta_j(Z_i)/\beta_k(Z_i)} described below, or \code{"population"} for
the debiased population ratio \eqn{\theta_j/\theta_k} (paper Section
2.3): the orthogonal-score average parameters with a delta-method
standard error and a Fieller interval (returned in \code{details}). The
\code{"population"} estimand ignores \code{trim}, \code{subgroup}, and \code{which_beta}.}

\item{trim}{Length-2 numeric with the lower and upper quantiles at
which the raw ratio is clipped.  Default \code{c(0.01, 0.99)}.}

\item{subgroup}{Optional logical / integer / Z column-name vector
selecting a subset of rows.  See Details.}

\item{which_beta}{Either \code{"hybrid"} (default, Stage-2-refined betas) or
\code{"dnn"} (raw Stage-1 cross-fitted DNN betas).  When the fit was made
with \code{stage2 = "none"}, the two are numerically identical.}
}
\value{
An \code{sc_quantity} with scalar \code{estimate}, clustered \code{se},
normal-approx CI, and \code{details} including the prototype delta SE.
}
\description{
Computes the respondent-averaged trimmed ratio
\eqn{r_i = \beta_j(Z_i) / \beta_k(Z_i)} between a \code{numerator} and
\code{denominator} attribute level, trimming the raw ratio at the
user-specified quantiles.  The SE is clustered at the respondent
level.
}
\details{
Point estimate and trimming follow the prototype
\verb{07b_structural_quantities.R} (lines 54--112).  The clustered SE
is new in sconjoint v0.1 (the prototype reports a plain delta-method
SE which is retained inside \verb{$details$delta_se} as a diagnostic).
}
\examples{
\dontrun{
fit <- scfit(y ~ a1 + a2 + a3 | z1 + z2, data = dat,
             respondent = "rid", task = "tid", profile = "pos",
             seed = 1L)
sc_mrs(fit, numerator = "a1", denominator = "a2")
}
}
