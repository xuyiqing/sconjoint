% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-posterior.R
\name{scmix_posterior}
\alias{scmix_posterior}
\title{Respondent-level posterior means and SDs from an scmix fit}
\usage{
scmix_posterior(fit, what = c("mean", "sd"))
}
\arguments{
\item{fit}{An \code{scmix} object.}

\item{what}{Which summaries to compute: \code{"mean"}, \code{"sd"}, or both.
The SD pass costs one extra sweep over the quadrature weights and
is skipped unless requested.}
}
\value{
An object of class \code{scmix_posterior}: a list with
\code{respondent} (ids in first-appearance order), \code{mean} (N x p, one
row per respondent, columns named by \code{fit$attr_names}), \code{sd} (same
shape, or \code{NULL} when not requested), \code{T_i}, \code{fold}, \code{attr_names},
and \code{n_respondents}.  \code{as.data.frame()} returns the wide form
(\code{respondent}, \code{T_i}, \code{fold}, \verb{mean_*}, \verb{sd_*}) for plotting and
validation joins.
}
\description{
Extracts \code{E[beta_i | data]} (and, on request, the posterior SD of
each coordinate) for every respondent, evaluated at the out-of-fold
\code{mu_hat(Z_i)} and the fold loading matrix on the quadrature grid.
}
\details{
The posterior mean combines \code{mu_hat(Z_i)} with the respondent's own
choices and is a shrinkage estimate by construction; its dispersion
understates the population's.  Use these summaries for descriptive
displays (labeled as such) and for external-validation correlations,
which are scale- and shrinkage-tolerant.  Population distributional
claims go through \code{\link[=scmix_polarization]{scmix_polarization()}} / \code{\link[=scmix_signshare]{scmix_signshare()}}; the
posterior SDs condition on the estimated \verb{(mu, A)} and do not
propagate nuisance uncertainty.
}
