% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantities-inequality.R
\name{sc_inequality}
\alias{sc_inequality}
\title{Preference inequality across respondents}
\usage{
sc_inequality(
  object,
  measure = c("variance", "gini"),
  subgroup = NULL,
  which_beta = c("hybrid", "dnn")
)
}
\arguments{
\item{object}{An \code{sc_fit}.}

\item{measure}{One of \code{"variance"} or \code{"gini"}.}

\item{subgroup}{Optional row selector.}

\item{which_beta}{Either \code{"hybrid"} (default) or \code{"dnn"}. See \code{?sc_mrs}.}
}
\value{
An \code{sc_quantity} with a data.frame estimate containing
per-attribute inequality measures.
}
\description{
Quantifies how heterogeneous preferences are across respondents
for each attribute dummy.
}
\details{
With \code{measure = "variance"} (default), reports
\eqn{\mathrm{Var}(\hat\beta_k(Z_i))} for each attribute dummy
\eqn{k} over respondents in the subgroup.

With \code{measure = "gini"}, reports the Gini coefficient of the
absolute values \eqn{|\hat\beta_k(Z_i)|} for each dummy.

When the fit carries an attribute-interaction term
(\code{scfit(..., interactions != "none")}), this quantity is computed from
the per-respondent \code{beta_i} exactly as before, but \code{beta_i} is then
the MAIN-EFFECT part of the utility (the coefficient on \code{deltaX} at
the no-interaction baseline), not the all-else-equal effect.
}
\section{Population claims}{

This function describes the fitted respondent-level (MAP) estimates.
Under the estimand-estimator correspondence it is limited to
descriptive use of those fitted values: population sign shares and
heterogeneity magnitudes require the integrated-likelihood route
(\code{\link[=scmix_polarization]{scmix_polarization()}} and its design checks), and MAP fractions
are biased for population shares (9--13 percentage points in the
head-to-head simulations, with no standard errors).
}

