% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantities-average.R
\name{sc_average}
\alias{sc_average}
\title{Average marginal effects on the logit or probability scale}
\usage{
sc_average(
  object,
  scale = c("logit", "probability"),
  subgroup = NULL,
  which_beta = c("hybrid", "dnn")
)
}
\arguments{
\item{object}{An \code{sc_fit}.}

\item{scale}{One of \code{"logit"} or \code{"probability"}.}

\item{subgroup}{Optional row selector.  With \code{scale = "logit"}, a
non-\code{NULL} subgroup returns the influence-function subgroup estimate:
the mean of the task-level orthogonal-score contributions within the
subgroup, with a respondent-clustered standard error.  This is the
debiased subgroup average of \eqn{f(Z)} (the construction used for the
party-level estimates in the paper's candidate application), not the
subgroup mean of the MAP \eqn{\hat\beta_i}.}

\item{which_beta}{Either \code{"hybrid"} (default) or \code{"dnn"}. See \code{?sc_mrs}.
Only affects \code{scale = "probability"}; the \code{"logit"} scale uses
\code{object$theta} from the DML fit (or its influence-function subgroup
version when \code{subgroup} is supplied).}
}
\value{
An \code{sc_quantity} with a data.frame estimate containing
per-attribute AMEs and clustered SEs.
}
\description{
With \code{scale = "logit"} (default), returns the DML coefficient
\eqn{\hat\theta} with its clustered SE — equivalent to
\code{coef(object)} but wrapped in an \code{sc_quantity}.
}
\details{
With \code{scale = "probability"}, computes average marginal effects:
\deqn{\mathrm{AME}_k = \frac{1}{N_S}\sum_{i\in S}
  \hat\beta_k(Z_i)\,G'(\Delta X_i^\top \hat\beta(Z_i))}
where \eqn{G'(x) = \mathrm{plogis}(x)(1 - \mathrm{plogis}(x))}.
This requires the stored \code{deltaX} matrix.

When the fit carries an attribute-interaction term
(\code{scfit(..., interactions != "none")}), the \eqn{G'} weights in the
\code{"probability"} scaling are computed from the main-effect linear
predictor \eqn{\Delta X^\top \hat\theta} only -- the per-task
interaction offset is omitted from the curvature evaluation -- and
the scaled coefficients remain main-effect quantities at the
no-interaction baseline.
}
