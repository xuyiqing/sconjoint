% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantities-direction-intensity.R
\name{sc_direction_intensity}
\alias{sc_direction_intensity}
\title{Direction-vs-intensity decomposition of the preference vector}
\usage{
sc_direction_intensity(object, subgroup = NULL)
}
\arguments{
\item{object}{An \code{sc_fit}.}

\item{subgroup}{Row selector.}
}
\value{
An \code{sc_quantity_bivariate} carrying two \code{sc_quantity}
sub-objects (\code{direction} and \code{intensity}) and a \code{separation}
data.frame in \verb{$details}.
}
\description{
For each dummy column \eqn{j}, reports:
\itemize{
\item \strong{direction} \eqn{d_j = \overline{\mathrm{sign}(\hat\beta_j(Z_i))}
    \in [-1, 1]} — positive when most respondents favor the level,
negative when most oppose, zero when evenly split.
\item \strong{intensity} \eqn{u_j = \overline{|\hat\beta_j(Z_i)|} \ge 0} —
how strongly the population cares about the level on average.
\item a scalar \strong{separation} diagnostic \eqn{s_j = u_j(1 - |d_j|)},
large when respondents care a lot but disagree on direction.
}
}
\details{
Both components carry respondent-clustered SEs.  This is the
headline conceptual contribution described in the paper (§2.3,
§3.1); it is new in sconjoint v0.1 with no prototype counterpart.
}
