% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baselines.R
\name{sc_baseline_logit}
\alias{sc_baseline_logit}
\title{Baseline logit model for comparison with \code{sc_fit}}
\usage{
sc_baseline_logit(object)
}
\arguments{
\item{object}{An \code{sc_fit} (must have \code{deltaX} and \code{y} stored).}
}
\value{
An object of class \code{"sc_baseline"} with components
\code{coefficients}, \code{vcov}, \code{fitted.values}, \code{residuals},
\code{model} (the underlying \code{glm} or \code{lm} object), and metadata.
}
\description{
Fits a standard logistic regression on the same \code{deltaX} and \code{y}
stored in an \code{sc_fit} object.  This produces homogeneous
coefficients (no heterogeneity in \eqn{\beta}) for comparison
against the DNN-estimated heterogeneous \eqn{\hat\beta(Z_i)}.
Standard errors are respondent-clustered.
}
