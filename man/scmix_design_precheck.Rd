% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-diagnostics.R
\name{scmix_design_precheck}
\alias{scmix_design_precheck}
\title{Hypothesized-value design check (pre-fielding)}
\usage{
scmix_design_precheck(
  deltaX,
  mu,
  A,
  T_i,
  N,
  n_nodes = 31L,
  n_bins = 40L,
  M = 2000L,
  seed = 1L,
  eig_tol = 0.05,
  t_min = 2
)
}
\arguments{
\item{deltaX}{Numeric matrix of planned attribute contrasts (the
pool tasks are resampled from), one column per attribute dummy;
column names become the coordinate labels.}

\item{mu}{Hypothesized mean coefficient vector of length p, or an
N x p matrix of scenario means.}

\item{A}{Hypothesized p x q loading matrix (\code{q <= 3}).}

\item{T_i}{Tasks per respondent: a scalar or a length-N vector
(unbalanced designs split simulation bins by distinct counts).}

\item{N}{Planned number of respondents.}

\item{n_nodes}{Gauss-Hermite nodes per residual dimension.}

\item{n_bins, M, seed}{Controls for the model-implied information
simulation (see \code{.scmix_information()}).}

\item{eig_tol}{Relative eigenvalue below which a loading direction
is reported as weakly identified (default 0.05).}

\item{t_min}{Loading t-ratio below which a coordinate is reported as
not supporting distributional claims (default 2).}
}
\value{
An \code{scmix_design_check} object with \code{source = "hypothesized"}.
}
\description{
The design-side counterpart of \code{\link[=scmix_design_check]{scmix_design_check()}}: instead of a
fitted model, it takes hypothesized values -- a mean vector (or
scenario matrix) \code{mu}, a loading matrix \code{A}, the planned contrast
pool \code{deltaX}, task counts \code{T_i}, and the planned number of
respondents \code{N} -- and reports which loading coordinates that design
would identify.  Run it before fielding: a design that cannot
identify the loadings it needs cannot support distributional claims,
whatever the data later say.  \code{N} scales the loading t-ratios, so
doubling the planned sample raises every t by roughly sqrt(2).
}
