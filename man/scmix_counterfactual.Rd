% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-inference.R
\name{scmix_counterfactual}
\alias{scmix_counterfactual}
\title{Debiased eta-integrated counterfactual choice share}
\usage{
scmix_counterfactual(fit, contrast, n_bins = 40L, M = 2000L, seed = 1L)
}
\arguments{
\item{fit}{An \code{scmix} object.}

\item{contrast}{One contrast or many: a numeric vector of length p
(attribute-dummy scale), a named subset expanded against
\code{fit$attr_names}, a numeric matrix with one contrast per row
(rownames become labels), or a list of such vectors.  All
contrasts in a call share one information simulation -- pass a
sweep as a batch rather than looping.  Each contrast carries its
own raw-share benchmark (\verb{$extra$raw}, one row per contrast).}

\item{n_bins, M, seed}{Controls for the model-implied information
simulation (see \code{.scmix_information()}).}
}
\description{
Estimates \verb{V(c) = E_Z[ E_u G(c' (mu(Z) + A u)) ]} -- the
heterogeneity-integrated share choosing profile bundle A over B for
a contrast vector \code{c}.  This is the "Jensen gap" object the
two-stage paper explicitly could not point-identify at fixed T; it
is identified under the maintained mixing law.  \code{H} integrates over
the quadrature grid; the mu-gradient is
\verb{sum_g w_g G'(c' beta_g) c}.
}
