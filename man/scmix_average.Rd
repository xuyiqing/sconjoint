% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-quantities.R
\name{scmix_average}
\alias{scmix_average}
\title{Debiased subgroup means from an scmix fit}
\usage{
scmix_average(fit, by, n_bins = 40L, M = 2000L, seed = 1L)
}
\arguments{
\item{fit}{An \code{scmix} object.}

\item{by}{The respondent grouping: a vector of length \code{N}
(respondents, in first-appearance order) or of length
\code{nrow(fit$deltaX)} (task rows; the first row per respondent is
used), or a length-1 character naming a moderator column in
\code{fit$Z}, in which case respondents are split at the median value
(groups \code{"above"} / \code{"at_or_below"}).}

\item{n_bins, M, seed}{Controls for the model-implied information
simulation (see \code{.scmix_information()}).}
}
\value{
An \code{scmix_quantity} with one entry per group x attribute
dummy, named \code{"<group>: <dummy>"}; \verb{$extra$groups} records group
sizes.
}
\description{
The integrated-likelihood analogue of \code{sc_average(scale = "logit", subgroup = ...)}: the estimand is \verb{theta_g = E[mu_k(Z_i) | G_i = g]}
for an observed respondent grouping G. The point estimate is the
within-group mean of the same corrected respondent-level signal that
\code{\link[=scmix_theta]{scmix_theta()}} averages (location correction plus loading-influence
term), and the SE is the within-group empirical standard error of
that signal. Groups must be observed respondent attributes (they are
not estimated), so no group-probability term enters the SE.
}
