% Generated by roxygen2: do not edit by hand
\name{scmix_refit_selected_matrix}
\alias{scmix_refit_selected_matrix}
\title{Refit one previously selected matrix specification}
\usage{
scmix_refit_selected_matrix(
  deltaX,
  y,
  Z,
  respondent_id,
  specification,
  integration_grid = NULL,
  preprocessing = NULL,
  n_epochs = 400L,
  learning_rate = 0.01,
  n_starts = 2L,
  mu_bound = 10,
  kappa_bound = 10,
  alpha_bound = 5,
  a_bound = 10,
  weight_bound = 10,
  opt_tol = 1e-07,
  grad_tol = 1e-04,
  nested_objective_tol = 1e-06,
  seed = NULL,
  device = "cpu",
  verbose = FALSE,
  source_analysis_signature = NULL
)
}
\arguments{
\item{deltaX}{Numeric task-level profile-contrast matrix.}
\item{y}{Binary task-level choices.}
\item{Z}{Numeric task-level moderator matrix, constant within respondent.}
\item{respondent_id}{Respondent identifier for each task row.}
\item{specification}{One normalized selected learner specification.}
\item{integration_grid}{Optional frozen finite integration grid.}
\item{preprocessing}{Optional frozen \code{Z} and \code{deltaX} transforms.}
\item{n_epochs}{Maximum optimization epochs.}
\item{learning_rate}{Adam learning rate.}
\item{n_starts}{Number of optimization starts.}
\item{mu_bound}{Bound for the conditional-mean output.}
\item{kappa_bound}{Bound for the position parameter.}
\item{alpha_bound}{Raw-coordinate bound for the unpenalized corrected-family
baseline.}
\item{a_bound}{Raw-unit Frobenius bound for the loading.}
\item{weight_bound}{Coordinatewise network-parameter bound.}
\item{opt_tol}{Objective stopping tolerance.}
\item{grad_tol}{Gradient tolerance.}
\item{nested_objective_tol}{Tolerance for checking that a corrected-family
refit attains the feasible pooled-reference objective.}
\item{seed}{Optional reproducibility seed.}
\item{device}{Torch device.}
\item{verbose}{Whether to report optimization progress.}
\item{source_analysis_signature}{Optional provenance signature from the tuning
analysis that selected the specification.}
}
\value{
An \code{scmix_tuned_matrix_fit} containing stored numeric estimates and a
portable \code{network_state}. It is marked as a fixed post-selection refit;
no candidate comparison or nested cross-validation is performed.
}
\description{
Fits exactly one supplied specification, optionally reusing frozen
preprocessing and numerical-integration inputs. This helper can supplement a
legacy artifact whose serialized Torch pointer cannot be reloaded, without
rerunning candidate selection. It does not replace nested tuning for formal
cross-fitted inference.
}
