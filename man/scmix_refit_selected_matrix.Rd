% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paperps-computation.R
\name{scmix_refit_selected_matrix}
\alias{scmix_refit_selected_matrix}
\title{Refit one previously selected matrix specification without retuning}
\usage{
scmix_refit_selected_matrix(
  deltaX,
  y,
  Z,
  respondent_id,
  specification,
  integration_grid = NULL,
  preprocessing = NULL,
  n_epochs = 400L,
  learning_rate = 0.01,
  n_starts = 2L,
  mu_bound = 10,
  kappa_bound = 10,
  alpha_bound = 5,
  a_bound = 10,
  weight_bound = 10,
  opt_tol = 1e-07,
  grad_tol = 1e-04,
  nested_objective_tol = 1e-06,
  seed = NULL,
  device = "cpu",
  verbose = FALSE,
  source_analysis_signature = NULL
)
}
\arguments{
\item{deltaX, y, Z, respondent_id}{Task-level matrix inputs.}

\item{specification}{One normalized selected specification containing
\code{hidden}, \code{weight_decay}, \code{q}, and integration controls.}

\item{integration_grid}{Optional frozen finite integration grid.}

\item{preprocessing}{Optional list with stored \code{Z} and \code{deltaX} transforms.}

\item{a_bound}{Positive raw-coefficient Frobenius bound for the loading.}

\item{weight_bound}{Positive coordinatewise network-parameter bound.}

\item{source_analysis_signature}{Optional signature of the tuning analysis
that selected \code{specification}; retained only as provenance.}
}
\value{
An \code{scmix_tuned_matrix_fit} marked as a fixed post-selection refit,
including a portable \verb{$network_state}.
}
\description{
This is a post-fit reproducibility helper. It fits exactly one fixed learner
specification on all supplied respondents, optionally using the frozen
preprocessing and integration grid from an earlier tuning result. It never
compares candidates and therefore cannot replace nested tuning for model
selection or cross-fitted inference. Its principal use is to supplement a
legacy fit artifact with a portable \code{\link[=scmix_restore_network]{scmix_restore_network()}} state.
}
