% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantities-wtp.R
\name{sc_wtp}
\alias{sc_wtp}
\title{Willingness-to-pay: signed MRS against a cost attribute}
\usage{
sc_wtp(
  object,
  attr,
  cost_attr,
  estimand = c("individual", "population"),
  trim = c(0.01, 0.99),
  subgroup = NULL,
  which_beta = c("hybrid", "dnn")
)
}
\arguments{
\item{object}{An \code{sc_fit}.}

\item{attr}{Numerator attribute level.}

\item{cost_attr}{Single-column cost attribute.}

\item{estimand}{Either \code{"individual"} (default) for the per-respondent
sign-flipped MRS, or \code{"population"} for the debiased population WTP
\eqn{-\theta_{\mathrm{attr}}/\theta_{\mathrm{cost}}} (paper Section
2.3): orthogonal-score average parameters with a delta-method standard
error and a Fieller interval. The \code{"population"} estimand ignores
\code{trim}, \code{subgroup}, and \code{which_beta}.}

\item{trim, subgroup}{Forwarded to \code{sc_mrs()}.}

\item{which_beta}{Either \code{"hybrid"} (default) or \code{"dnn"}. Forwarded to
\code{sc_mrs()}. See \code{?sc_mrs} for details.}
}
\value{
An \code{sc_quantity} with the sign-flipped MRS.
}
\description{
Thin wrapper around \code{\link[=sc_mrs]{sc_mrs()}}: \code{sc_wtp(object, attr, cost_attr)}
returns \eqn{-\widehat{\mathrm{MRS}}_{j,k}} where \eqn{j} is the
\code{attr} column and \eqn{k} is the \code{cost_attr} column.  The minus
sign converts the MRS "units of cost to maintain indifference
when \code{attr} increases by 1" into a positive willingness-to-pay
when the cost attribute has a negative utility coefficient.
}
\details{
\code{cost_attr} must resolve to a single dummy column (either a
numeric attribute or a single non-reference level of a binary
factor).  Multi-level cost attributes require calling \code{sc_mrs()}
directly with the user-chosen level pair.

When the fit carries an attribute-interaction term
(\code{scfit(..., interactions != "none")}), the WTP -- like the MRS it
wraps -- is a ratio of MAIN-EFFECT quantities and is
reference-profile-specific: the marginal rate at the no-interaction
baseline.  A \code{reference_profile} argument is future work; see
\code{\link[=sc_mrs]{sc_mrs()}}.
}
