% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantities-counterfactual.R
\name{sc_counterfactual}
\alias{sc_counterfactual}
\title{Counterfactual choice probability between two profiles}
\usage{
sc_counterfactual(object, A, B, subgroup = NULL)
}
\arguments{
\item{object}{An \code{sc_fit}.}

\item{A, B}{Named lists describing the two profiles.}

\item{subgroup}{Optional row selector, see \code{sc_mrs}.}
}
\value{
An \code{sc_quantity} with scalar estimate, clustered SE,
normal-approx CI.
}
\description{
For each row \eqn{i} in the (optional) subgroup, computes
\eqn{\hat p_i = G((X_A - X_B)^\top \hat\beta(Z_i))}, then averages
over \eqn{i}.  The SE is the respondent-clustered empirical SD of
the per-row probabilities (not an influence-function SE on the DML
\eqn{\hat\theta}); v0.1 does not propagate nuisance uncertainty
from \eqn{\hat\beta(Z)} into \eqn{\hat p}.
}
\details{
\code{A}, \code{B} are named lists in human-readable form, e.g.
\code{A = list(gender = "female", talent = "hard_working")}; attributes
not mentioned default to the reference level.  The conversion from
\verb{(A, B)} to encoded dummy vectors follows the prototype
\verb{07b_structural_quantities.R} lines 153--175 (\code{profile_to_dummies}).
}
