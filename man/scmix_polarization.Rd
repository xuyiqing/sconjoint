% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-inference.R
\name{scmix_polarization}
\alias{scmix_polarization}
\title{Debiased population sign shares (polarization) from an scmix fit}
\usage{
scmix_polarization(fit, n_bins = 40L, M = 300L, seed = 1L, sd_floor = 0.05)
}
\arguments{
\item{fit}{An \code{scmix} object.}

\item{n_bins, M, seed}{Controls for the model-implied information
simulation (see \code{.scmix_information()}).}

\item{sd_floor}{Lower bound on the conditional residual SD used in
\code{H = Phi(mu_k/sigma_k)}.  When \verb{(AA')_kk} is at or near zero the
sign-share functional degenerates to the step \verb{1\{mu_k(Z) > 0\}} and
is no longer smooth in \code{mu} --- the gradient \code{phi/sigma_k} blows up
and the orthogonal correction can push the estimate outside
\verb{[0, 1]}.  Coordinates floored here are reported in \verb{$extra$floored}
and should be read as "residual variance indistinguishable from
zero; the share is effectively the sign of the conditional mean."}
}
\description{
Estimates \code{pi_k = E_Z[ Pr(beta_ik > 0 | Z_i) ]} under the maintained
mixing law: conditional on Z, \code{beta_k ~ N(mu_k(Z), sigma_k^2)} with
\verb{sigma_k^2 = (AA')_kk}, so \code{H = Phi(mu_k(Z)/sigma_k)} and the
mu-gradient is \code{phi(mu_k/sigma_k)/sigma_k * e_k}.  In the two-stage
framework this quantity had no orthogonal score and was reported as
a shrinkage-biased MAP sign fraction; under the maintained mixing
law it is an identified smooth functional with debiased inference.
}
\details{
\code{sigma_k} uses each respondent's fold-specific loading matrix
(cross-fitted); its estimation error is not yet in the SEs (see the
file header).  A fold-spread range for \code{sigma_k} is returned in
\verb{$extra} as a sensitivity check.
}
