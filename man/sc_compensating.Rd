% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantities-compensating.R
\name{sc_compensating}
\alias{sc_compensating}
\title{Compensating differential between two attribute levels}
\usage{
sc_compensating(object, benefit, cost, trim = c(0.01, 0.99), subgroup = NULL)
}
\arguments{
\item{object}{An \code{sc_fit}.}

\item{benefit}{Dummy identifier for the benefit attribute level.}

\item{cost}{Dummy identifier for the cost attribute level.}

\item{trim}{Length-2 numeric, lower and upper quantile cut-offs
for trimming the per-row ratio.  Defaults to \code{c(0.01, 0.99)}.}

\item{subgroup}{Optional subgroup selector (see
\code{sc_subgroup}).}
}
\value{
An \code{sc_quantity}.  \code{estimate} is a scalar; \code{details}
contains \code{frac_compensated}, \code{n}, and the trimming thresholds.
}
\description{
For a "benefit" dummy and a "cost" dummy (identified by name or
index), compute the per-respondent compensating differential
\eqn{c_i = -\hat\beta_{\mathrm{benefit}}(Z_i)/\hat\beta_{\mathrm{cost}}(Z_i)}
-- the (signed) quantity of the cost attribute that offsets a
one-unit gain in the benefit attribute on the utility scale.  The
returned object carries three summaries:
\itemize{
\item \code{estimate} -- the respondent-average compensating
differential, trimmed at the user-supplied quantiles;
\item \code{se} -- respondent-clustered empirical SE of the trimmed
per-row ratio;
\item \code{frac_compensated} -- the fraction of respondents for whom
\code{beta_benefit + beta_cost >= 0} (the "fraction offset" from
spec §3.3 B2).
}
}
\details{
Dummy identifiers follow the same rules as \code{sc_mrs}: either
\code{"attribute:level"}, a bare dummy name matching \code{object$attr_names},
or an integer column index.
}
