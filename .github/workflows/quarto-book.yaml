# Quarto book deploy - activated in M6.
# During M1 (skeleton) this workflow is a no-op placeholder so the
# file exists in source control and can be filled in later.

on:
  workflow_dispatch:

name: quarto-book

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Quarto book workflow will be activated in milestone M6."
